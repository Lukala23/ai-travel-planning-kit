interface:
  display_name: "AI Travel Planning Kit"
  short_description: "Research and plan only the travel scope needed now"
  default_prompt: "Use $plan-reliable-trips-en to do the smallest sufficient research for my current travel question, expanding only when I request a complete plan."
policy:
  allow_implicit_invocation: false
