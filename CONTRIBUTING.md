# Contributing to AI Travel Planning Kit

[English](CONTRIBUTING.en.md)

感谢你帮助 **AI 旅行规划工具包（AI Travel Planning Kit）**变得更可靠、更简洁、更适合真实旅行。

## 什么样的贡献最有价值

- 真实旅行中可复现的失败案例；
- 某个国家 / 地区的支付、交通、税费、语言或安全核验方法；
- 更高质量的官方、专业或本地来源体系；
- 对住宿、餐饮、航空与机票、自驾、健康医疗、旅行保险、博物馆、摄影或徒步规则的实测改进；
- 删除重复、僵硬、难懂或无助于决策的约束；
- 更清晰的任务卡、交付结构和 AI 协作方法；
- 修复文档链接、术语不一致和加载路由错误。

## 提交前先判断规则类型

请说明你的建议属于：

- 安全 / 法律底线；
- 长期硬约束；
- 默认偏好；
- 软偏好；
- 单次出行参数；
- 某一目的地才适用的动态信息。

不要把单一旅行经历直接写成全世界通用规则。先描述适用条件和不适用边界。

## Issue 建议包含

1. 发生了什么；
2. 当前规则为什么没有处理好；
3. 可复现的旅行场景；
4. 建议修改的行为，而不只是建议增加篇幅；
5. 支持结论的直接来源；
6. 动态信息的发布日期或查证日期；
7. 是否涉及安全、法律或高成本不可退项目。

请勿提交护照、证件、银行卡、支付信息、订单二维码、账号、手机号、详细住址或未脱敏的完整行程。

## Pull Request

尽量保持一个 PR 解决一个主题。PR 描述应包含：

- 修改内容；
- 修改原因；
- 对使用者和模型行为的影响；
- 变更涉及的文件；
- 使用的验证方法；
- 仍未解决的限制。

修改详细规则时，同步检查：

- `SKILL.md` 的条件加载矩阵是否仍能找到该规则；
- 该规则是否放在唯一负责该主题的专项文件中，是否误建了长期用户画像或重复规则；
- 专项检查是否留在负责该主题的模块中；`output-contract.md` 只保留跨任务的最小检查，不复制专项清单；
- `trip-brief-template.md` 是否真的需要新增字段；
- `docs/architecture.md` 是否需要更新加载关系；历史讨论确有长期价值时再记录到 `constraint-review.md`；
- 新增 / 删除 reference 时，`scripts/build-release-assets.sh` 的场景包与完整归档清单是否同步；
- 中文与英文版本是否仍保持相同的安全和决策逻辑；中文版保留中国出发定位，英文版保持地区中立，不要求逐句翻译。

## 写作原则

- 使用清楚、可执行的中文；
- 先写行为和适用条件，再写原因；
- 不用抽象术语替代真实判断；
- 不把热度、评分或单一平台排序写成事实；
- 不编造网址、票价、坐标、班次和所谓官方意见；
- 来源冲突时保留冲突；
- 保持软偏好为软偏好；
- 优先简化，不以文件和规则数量衡量贡献。

## 验证

提交前至少完成：

- 检查 Markdown 链接；
- 搜索是否残留旧术语或冲突规则；
- 确认没有本地绝对路径和敏感信息；
- 运行 `scripts/validate-project.sh` 检查 Skill 结构、目录、链接、场景包和路由用例；
- 任一共享规则变化时同时校验中文、英文 Skill，并重建双语 Release 资产；
- 触发或路由变化时更新 `tests/routing-cases.tsv`，并至少用一个窄范围真实提示观察是否误载模块；
- 阅读完整 diff，确认只包含本次主题。

## License

提交贡献即表示你同意按本仓库的 [MIT License](LICENSE) 许可你的贡献。
